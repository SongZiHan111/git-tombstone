# {项目名} 导航（AGENTS.md）

> 一句话项目定位。本文件是项目入口，agent 会自动读取。
> **改任何代码前先读文档地图。**

## 文档地图（权威指向，勿另立门户）

| 文档 | 什么时候读 |
|---|---|
| ARCHITECTURE.md | 改模块结构/依赖/数据流前必读 |
| TESTING.md | 测试清单与工具 |
| API_VERIFICATION.md | 用任何新 SDK API 前查核验状态 |
| HANDOVER-YYYYMMDD-*.md | 当前方向/待办，接续会话必读（最新事实源） |
| docs/DEADENDS.md | 开工前扫一眼死路账，防重蹈 |

## 模块速览

（用 10 行以内说清目录结构和依赖铁律）

## 记忆规约（本项目记忆 = 代码 + git + 死路账，不设独立记忆系统）

1. （放你的领域铁律，如：未核验的 API 不得进代码）
2. （放你的架构铁律）
3. 历史架构决策属档案，勿据其恢复代码
4. **死路账（主墓碑）**：任何回退/放弃（无论有无 commit），执行者**当场同窗**在 docs/DEADENDS.md 追加一行：方案→死因→恢复指针→置信；行内日期可省，权威日期 = git blame；未验证候选不入账；超 60 行时提议归档
5. **revert 墓碑（冗余通道）**：回退类 commit 以 `revert: 方案 → 死因` 开头
6. **真空期防护**：每轮拍板/转向后助手顺手 `git add -A && git commit -m "wip: 一句话"`（救命优先；日期门禁钩子在 .git/hooks/pre-commit，重克隆需重装）
7. **转向协议**：闻"不搞 X 改做 Y"→ **先落盘再干活**：X 证伪部分入死路账，其余在 HANDOVER 标 `parked(日期)`；大换血则新建 HANDOVER-YYYYMMDD，旧文件首行标"已被取代"
8. **新鲜度**：会话首读跑 `date`；HANDOVER 或任何"已定"距今 >7 天 → 降级为"待重议候选"，先问用户
9. **灰色纪律**：未验证事项只以"问题/候选"口吻写 HANDOVER，禁止结论口吻；"已定"必须带核验指针或显式"未验证"标注
10. **禁读外部账本**：其他项目/目录的记忆文件不得作为事实源
11. **决策护栏**：用户说"你决定"时，结论末尾必附"最强反方一句 + 证伪信号"；不可逆且反方苍白时，建议用更强模型做单轮第二意见
